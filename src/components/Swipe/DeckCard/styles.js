import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    minHeight: 355,
  },
  titleStyle: {
    fontSize: 25,
  },
  subtitleStyle: {
    fontSize: 20,
  },
  textStyle: {
    marginBottom: 10,
    textAlign: 'center',
  },
  genreStyle: {
    textAlign: 'center',
    marginBottom: 10,
    fontSize: 16,
    fontWeight: '600',
  },
});

export default styles;
